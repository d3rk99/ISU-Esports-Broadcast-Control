# Companion module for ISU Esports Broadcast Control

Private Bitfocus Companion 5 connection module for the ISU Esports Broadcast Control desktop application.

Build and package with:

```powershell
corepack yarn install
corepack yarn package
```

The generated `.tgz` can be imported from Companion's **Modules → Import module package** screen.
